<%= form_with(model: event_realizers_title, local: true) do |form| %>
  <% if event_realizers_title.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_realizers_title.errors.count, "error") %> prohibited this event_realizers_title from being saved:</h2>

      <ul>
      <% event_realizers_title.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
