<%= form_with(model: the_speaker, local: true) do |form| %>
  <% if the_speaker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(the_speaker.errors.count, "error") %> prohibited this the_speaker from being saved:</h2>

      <ul>
      <% the_speaker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Título %>
    <%= form.text_field :title , class: "input-texto" %>
  </div>

  <div class="field">
    <%= form.label :Descrição %>
    <%= form.text_area :description %>
  </div>

  imagem
  <%= form.file_field :image, multiple: true %>

  documento pra landing page (pdf)
  <%= form.file_field :document, multiple: true %>
  <div class="actions">
    <%= form.submit "Atualizar" %>
  </div>
<% end %>
