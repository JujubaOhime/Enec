<%= form_with(model: the_speaker, local: true) do |form| %>
  <% if the_speaker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(the_speaker.errors.count, "error") %> prohibited this the_speaker from being saved:</h2>

      <ul>
      <% the_speaker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Título %>
    <%= form.text_field :title , class: "input-texto" %>
  </div>

  <div class="field">
    <%= form.label :Descrição %>
    <%= form.text_area :description %>
  </div>


  <div class="field send-file" id="image_field">
    <%= form.file_field :image, id:"image_label"%>
    <label for="image_label">Escolher Imagem</label>
    <%= form.hidden_field :picture_cache %>
  </div>

  <div class="field send-file" id="document_field">
    <%= form.file_field :document, id:"document_label"%>
    <label for="document_label">Escolher Documento</label>
    <%= form.hidden_field :document_cache %>
  </div>

  <div class="actions">
    <%= form.submit "Atualizar" %>
  </div>

<% end %>


